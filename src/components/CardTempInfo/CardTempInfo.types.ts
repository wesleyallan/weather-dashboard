export type CardTempInfoProps = {
  title: string;
  icon: string;
  measurement: string;
  children: React.ReactNode;
}

export type TitleProps = {
  bold: boolean;
  secondary: boolean;
}

export type TextProps = {
  secondary: boolean;
}